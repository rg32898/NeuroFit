# Workspace

## Overview

pnpm workspace monorepo using TypeScript. Each package manages its own dependencies.

## Stack

- **Monorepo tool**: pnpm workspaces
- **Node.js version**: 24
- **Package manager**: pnpm
- **TypeScript version**: 5.9
- **API framework**: Express 5
- **Database**: PostgreSQL + Drizzle ORM
- **Validation**: Zod v3 (workspace catalog pin) — `drizzle-zod` removed (v0.8 needs Zod v4)
- **Auth**: JWT access + refresh tokens, bcryptjs (12 rounds), `tokenVersion` for server-side refresh revocation
- **Shared schemas**: hand-written cross-app Zod schemas live in `lib/shared` (`profile`, `workout` subpaths). Do NOT use `lib/api-zod` — that's auto-generated.
- **Adaptive engine** (`api-server/src/services/adaptiveEngine.ts`): pure functions for RAISE/HOLD/LOWER decisions per FR-L8. RAISE only on TWO consecutive scores ≥70; LOWER on TWO consecutive ≤30 OR a `too_hard` newest event. Score deltas ±250 clamped server-side to [0, 5000] via atomic SQL.
- **Workout assembler** (`api-server/src/services/workoutService.ts`): pure `selectWorkoutGames`, picks 3–5 games, always includes focus domain, balances other domains by inverse 7-day play count, respects free-tier rotation, soft 12-min target with hard 3-game floor.
- **Workout routes** (`/api/workout/*`): GET `/today` (returns existing or creates), POST `/:id/complete` (validates gameIds against the planned session, rejects duplicates, applies engine + calls `streakService.recordCompletion`), POST `/signal-too-hard` (records signal + lowers).
- **Streak engine** (`api-server/src/services/streakService.ts`): the single authoritative streak path. `recordCompletion(userId, completedAt)` runs inside `db.transaction` with a `SELECT ... FOR UPDATE` lock to prevent lost-update races. State machine in UTC: diff=0 idempotent · diff=1 +1 · diff≥2 +1 if a freeze is available (consume one) else reset to 1 · diff<0 ignored. `MAX_FREEZES=2`, topped up monthly by the cron.
- **Progress + admin routes**: POST `/api/progress/events` (auth, batch ≤100, idempotent per `(userId, clientEventId)` via UNIQUE constraint, only fires the streak engine for *newly inserted* `game_completed` events) · GET `/api/progress/streak` · POST `/api/progress/restore-streak` (header `x-support-token` = `SUPPORT_TOKEN`, no JWT) · POST `/api/admin/cron/daily` (header `x-cron-secret` = `CRON_SECRET`, host-agnostic — Render/Fly/GitHub Actions/cron-job.org).
- **Production safety**: `config.ts` refuses to boot in `NODE_ENV=production` if `CRON_SECRET` or `SUPPORT_TOKEN` are still the dev placeholders.
- **Subscriptions** (FR-6.x): three providers — Apple `/verifyReceipt` (with sandbox 21007 retry), Google Play Subscriptions V2 (service account via `google-auth-library`), Stripe (server SDK + webhook signature verification). Plan catalog in `lib/shared/src/subscription.ts` (monthly $9.99 / yearly $79.99, 7-day trial, productIds `neurofit_{monthly,yearly}_v1`). `services/billing/index.ts` exports `validateAndSync(userId, input)` and `persistValidatedReceipt`. Mobile clients send store receipts to `POST /api/subscription/validate-receipt`; Stripe is webhook-driven. `cancelSubscription` only sets `cancelAtPeriodEnd=true` locally (the store is the source of truth for IAP — user must cancel in device settings). Audit trail in `billing_events` with UNIQUE on (provider, providerEventId) for replay-safe webhooks. `cancelAtPeriodEnd` is sticky: a SQL OR in upsert prevents stale provider webhooks from un-cancelling a user's intent.
- **Stripe webhook raw body**: `app.ts` mounts `/api/webhooks/stripe` with `express.raw({ type: "application/json" })` BEFORE the global `express.json()` so the HMAC signature stays verifiable. Don't move it.
- **Billing cron**: `POST /api/admin/cron/billing` (same `x-cron-secret` model as `/cron/daily`) runs FR-6.3 trial-end reminders (48h window, idempotent via `trial_reminder_sent_at`) + FR-6.4 receipt notifications (5-minute lookback, NOT EXISTS subquery on the notifications log).
- **Notification transport** (`services/notifications`): vendor-neutral. `sendEmail` tries Resend HTTP → SMTP (nodemailer) → log; `sendPush` tries Expo Push HTTP → log. Every call appends a row to the `notifications` table — the table is now a **send LOG** (not a queue) with `channel`, `template`, `recipient`, `status`, `error` columns. FR-8.4 enforced as a hard guard in `sendEmail`: any send with `marketing: true` is blocked unless `marketingOptIn: true` is also passed (logged with `error="marketing_send_without_optin"`). Transactional sends (receipts, trial reminders, support acks) bypass the guard by omitting the marketing flag.
- **Reports + Support**: `POST /api/reports/content` is idempotent within 24h per (reporter, gameItem) — duplicate returns the original with `idempotent: true` and 200 (vs 201 on fresh create). `POST /api/support/tickets` always sends a transactional ack email when an email is on file. `GET /api/support/tickets/:id` returns 404 for both "missing" and "owned by someone else" (no id-enumeration leak).
- **Push tokens** (`push_tokens` table): keyed by Expo push token (unique globally — re-registration moves the token to the new userId). Registration endpoint is a future prompt; `sendPush` reads what's there and gracefully degrades to log when empty.
- **Mobile shell** (`artifacts/mobile`, Expo SDK 54 + Expo Router): Prompt 11 added the cross-cutting infra under `src/` — `theme/` (light+dark tokens, spacing, radii, typography, shadows; `useTheme` hook), `components/ui/{Button,Card,Input,Screen,Text}` minimal primitives (no third-party UI kit), `lib/api.ts` typed fetch wrapper (reads `EXPO_PUBLIC_API_URL`, attaches Bearer from secure-store, single-flight refresh on 401, structured `ApiError`, forced-logout listeners), `lib/auth-store.ts` Zustand store (login/logout/restoreFromStorage/hydrated gate), `lib/tokenStorage.ts` (expo-secure-store on native, localStorage fallback on web), `i18n/` (i18next + expo-localization, `en` only for now), and a placeholder `screens/WelcomeScreen.tsx`. `app/_layout.tsx` runs `restoreFromStorage()` + `initI18n()` at mount and gates render until both resolve. Existing Expo Router screens kept unchanged. Path alias `@app/*` → `./src/*` added.
- **Mobile metro config**: `metro.config.js` adds a resolver `blockList` for `node_modules/.pnpm/**/_tmp_*/**` so transient pnpm install dirs don't crash Metro's file watcher with ENOENT. Disables `watcher.healthCheck` for the same reason.
- **Mobile artifact.toml**: `ensurePreviewReachable` is intentionally omitted. The Replit expo-domain proxy returns 502 for `https://$REPLIT_EXPO_DEV_DOMAIN/...` even when Metro is confirmed serving 200 on `localhost:18115/status` — this is a platform-side proxy routing issue, not a Metro issue. With the HTTP probe disabled, the workflow correctly stays RUNNING based on process liveness. Real testing happens via Expo Go on a device using the QR code / `exp://` URL Metro prints, which routes correctly.
- **Export hardening** (Prompt 20, final): Repo is now reproducible on a fresh laptop with `git + Node 20 + pnpm 9 + Docker`. Added: root aggregate `.env.example` (single-place reference for every var across the three artifacts); `docker-compose.yml` with `postgres:16-alpine` + `api` + `admin` services and a healthcheck on Postgres; multi-stage `artifacts/api-server/Dockerfile` (builder runs `pnpm install --frozen-lockfile` + `typecheck:libs` + `pnpm --filter api-server build`, then `pnpm deploy --prod` into `/app`; runner is `node:20-alpine` + `tini` + non-root user shipping `dist/` + production `node_modules` + `lib/db/migrations`); `artifacts/admin/Dockerfile` (Next 14 standalone output, runs `node artifacts/admin/server.js` on port 3100); `.dockerignore` excludes mockup-sandbox, mobile, and all .env files; `scripts/dev.sh` one-command bootstrap (copies `.env.example` → `.env` for root + each artifact, boots postgres, installs deps, migrates, seeds, prints "open three terminals" instructions); `.github/workflows/ci.yml` runs typecheck + tests for api/admin/mobile (mobile = jest only — no native build in CI per FR rationale); full `README.md` rewrite with TOC, stack table, env var tables linking to provider sign-up pages (Neon, Stripe, AdMob, Resend, Expo, Apple, Google), deployment recipes for Render/Fly/Railway/Vercel/EAS, and a "going to production" checklist; `docs/architecture.md` short doc covering auth, workout assembly, adaptive engine, streak, sync, ads, trust layer with FR-ID mapping; MIT `LICENSE`; `CHANGELOG.md` with one bullet per build prompt. Replit-ref scrub pass: removed Replit URL from `app.json` expo-router origin (now `http://localhost:8081`); replaced `artifacts/mobile` `dev` script with `node scripts/dev.js` wrapper that conditionally forwards `REPLIT_*` env vars when present (works on plain laptop too); reordered `artifacts/mobile/scripts/build.js` getDeploymentDomain() to check `EXPO_PUBLIC_DOMAIN` first (REPLIT_* are now annotated convenience fallbacks); rewrote `admin.ts` cron comment to be host-agnostic; added a 7-line disclaimer comment above the `@replit/*` catalog entries in `pnpm-workspace.yaml` explaining they're used only by mockup-sandbox (the optional canvas dev tool, excluded from CI/Docker/prod). Mobile dev now runs via `artifacts/mobile/scripts/dev.js` so the same script works on Replit and on a fresh laptop unchanged. Final test run: 57/57 mobile + green api/admin typecheck. **Pending — sandbox blocks `git tag` even as a lightweight ref**; user must run `git tag v0.1.0` manually from the shell to land the v0.1.0 release tag.
- **Offline + rewarded ads** (Prompt 19, FR-7.x / FR-9.x): Added `@react-native-community/netinfo` and `react-native-google-mobile-ads` to mobile (both standard, no Replit deps). New modules — `src/lib/offline-cache.ts` pre-downloads catalogue + per-game items on app foreground (single-flight + 30s debounce + 3-day TTL via `nf_offline_*_v1` AsyncStorage keys, bounded concurrency 3); `src/lib/ads.ts` exposes one API `showRewardedAd(reasonCode) → Promise<boolean>`, dynamically `require`s the AdMob SDK so jest/Expo Go don't crash, uses Google's official test ad units in `__DEV__` and `EXPO_PUBLIC_ADMOB_{ANDROID,IOS}_REWARDED_UNIT` in prod, and enforces FR-7.4 (cap 1 reward per app-open session) + FR-7.5 (no ad while a workout is active) + FR-7.7 (premium short-circuit BEFORE SDK init via `configureAds({isPremium})` wired from root layout reading the React Query cache for `["subscription","status"]`); `src/lib/workout-state.ts` is the tiny module-level flag that WorkoutRunnerScreen toggles on mount/unmount. `progress-queue.ts` extended with a NetInfo edge-trigger: when connectivity transitions offline→online we reset the backoff and immediately flush — events keep their original `clientEventId` so the server's existing `(userId, clientEventId)` UNIQUE constraint enforces FR-9.3 merge (never last-write-wins). GameContainer's items query has `retry: false` and falls back to `getCachedItems(slug)` on any non-401/404 failure. Train tab shows an opt-in "Watch ad to unlock today's premium game" CTA only for free users (hidden for active/trialing/grace), only fires `showRewardedAd` on explicit tap. Tests: `OfflineSync.test.ts` (offline mid-workout preserves event + reconnect replays same clientEventId; triple-flush still POSTs the event exactly once) and `Ads.test.ts` (cap returns false on 2nd attempt; workout-active blocks; premium short-circuits without calling `initialize`) — 5/5 new + 52/52 existing mobile tests passing. **Sequence diagram** of offline→online sync: `[user finishes game] → enqueue() → AsyncStorage.write(nf_progress_queue) → flush() (fire-and-forget) → api.post fails (offline) → backoff scheduled, queue intact ─── [airplane mode off] → NetInfo emits isConnected:true → edge-trigger detects offline→online → attempt=0, retry timer cleared → flush() → api.post('/api/progress/events', {events:[...]}) → 200 → remove flushed events by clientEventId → server INSERT ... ON CONFLICT (userId, clientEventId) DO NOTHING (FR-9.3 merge, never last-write-wins) → next foreground or NetInfo edge re-runs flush() if anything left`.
- **Admin console** (Prompt 18, FR-12.x): Backend gained `users.role` (text, default "user") and a new `audit_logs` table (actor / action / targetType / targetId / metadata + indexes); `content_reports` extended with `claimedById / claimedAt / resolvedAt / resolutionNote`. Role ladder lives in `lib/shared/src/admin.ts` (`ROLES = user < author < reviewer < admin`, `hasRole`, all zod request schemas) and is exported via the `@workspace/shared/admin` subpath. `requireRole(min)` middleware (`api-server/src/middlewares/requireRole.ts`) does one DB lookup per request (no role caching — revocation is immediate) and populates `req.actor`. New router `src/routes/adminConsole.ts` mounted at `/api/admin` alongside the cron `/api/admin` router (most-specific route wins): `/me`, `/games` CRUD (author+; admin to delete), `/items` CRUD (author drafts; reviewer publishes / hot-patches / unpublishes / deletes), `/reports` queue (reviewer claim/resolve/dismiss), `/audit` (admin only). FR-12.2/12.6 enforced server-side: an `author` patching `isPublished=true` on a game gets 403; only `reviewer+` can hit `/items/:id/publish`. FR-12.3 hot-patch bumps `version` and refreshes `reviewedById/reviewedAt` so mobile picks up content fixes on next catalogue refresh without an app-store update. Editing a published item via PATCH returns 409 `ITEM_NOT_DRAFT` to force the audit-trailed hot-patch path. Every mutation calls `recordAudit(...)` — append-only feed visible in /admin/audit. Admin frontend is a separate Next.js 14.2.18 (App Router, React 18.3.1, Tailwind, standalone output) artifact at `artifacts/admin/`, mounted under `/admin/` via `next.config.mjs` `basePath`. Built with **no Replit-specific deps** so it self-hosts on any Node host. The browser **never** holds the access token — login goes through a Next.js BFF route `/admin/api/auth/set-cookie` which calls upstream `/api/auth/login` then verifies `/api/admin/me` succeeds (rejecting non-admin users with 403) before issuing HttpOnly `admin_at` (1h, path-scoped to `/admin`) and `admin_rt` (30d) cookies. All client mutations go through `/admin/api/proxy/[...path]` which reads the HttpOnly cookie and forwards as a Bearer header to the api-server. `src/middleware.ts` redirects unauth requests to `/login?from=...`. Pages: login, games list/new/detail, items new/edit (with publish + hot-patch + unpublish controls), reports kanban (open/claimed/resolved), audit table. 5 admin BFF vitest tests + 9 admin backend vitest tests passing; full `pnpm run typecheck` clean. ⚠️ Two DB columns (`users.role`, `content_reports.claimed_by_id` etc) and the new `audit_logs` table must exist in production before deploy — push or rely on Publish-time schema diff.
- **Settings + trust layer** (Prompt 17, FR-6.x / FR-8.x): Backend gained `users.deletionScheduledAt` + `userRepo.{scheduleDeletion,cancelDeletion,getDeletionStatus}`; new routes `POST /api/auth/delete-me` (bumps `tokenVersion` to log all devices out), `POST /api/auth/undo-delete`, `GET /api/auth/deletion-status` (14-day reverse window — daily cron will purge after that). New `services/refundService.ts` (`computeRefundEligibility`) infers `lastChargeAt` from `currentPeriodEnd - planPeriod` (30d/365d) and exposes `GET /api/subscription/refund-eligibility` + `POST /api/subscription/refund-request` (logs `refund_requested` billing event with provider-specific instructions URL — Apple report-a-problem, Google Play refund page, Stripe in-app). Mobile gained `src/lib/settings-store.ts` (zustand + AsyncStorage persist; `marketingOptIn` defaults to **false** per FR-8.4; `notificationsMaster` cascades disable; relaxed mode / timer scale / font scale / high-contrast / colorblind palette), `src/lib/subscription-api.ts` (react-query hooks + `formatPrice` / `annualPerMonthEquivalent` helpers), `src/lib/account-api.ts`. Pure presentational screens under `src/screens/{settings,subscription}/` (`SettingsScreen`, `NotificationsScreen`, `AccessibilityScreen`, `PreferencesScreen`, `AccountDeletionScreen`, `PlansScreen`, `SubscriptionStatusScreen`). Wired via `app/settings/*` and `app/subscription/*` route stacks; the Profile tab (`app/(tabs)/profile.tsx`) renders `SettingsScreen`. Trust-layer invariants enforced + tested: (1) **cancel reachable in ≤2 taps** from Settings (Settings row → Cancel CTA), (2) **PlansScreen never auto-selects yearly** — `useMemo` locks default to "monthly", (3) **single Alert.alert confirm** on cancel + delete (no counter-offers / surveys), (4) Plans show full price, billing period, trial days, "cancel anytime", and per-month equivalent on yearly only, (5) marketing toggle starts off. ⚠️ DB migration for `deletion_scheduled_at` must run before deploy or `/auth/delete-me` will throw `column does not exist`.
- **Progress dashboard** (Prompt 16): `lib/shared/src/achievements.ts` is the pure achievement catalogue — `AchievementSnapshot` type, `scoreToBand` (4 bands at 1250/2500/3750 over the engine's 0–5000 score), 15 `ACHIEVEMENTS` predicates, and `computeAchievements(snapshot)`. Server `services/progressSummaryService.ts` aggregates streak + proficiency + 30-day daily completions + lifetime totals via `Promise.all` of 5 queries → `GET /api/progress/summary` (requireAuth). Daily counts use `to_char(createdAt AT TIME ZONE 'UTC', 'YYYY-MM-DD')` group-by; service zero-fills the 30-entry timeline. Mobile Progress tab (`src/screens/progress/ProgressScreen.tsx` + `app/(tabs)/stats.tsx` thin wrapper) renders streak header, react-native-svg `Spark` chart, 6 domain proficiency bars (Beginner→Expert), totals, and unlocked-achievement cards via `useProgressSummary` react-query hook in `src/lib/progress-api.ts`. All copy goes through `useTranslation` with keys under `progress.*` in `src/i18n/locales/en.ts`.
- **API codegen**: Orval (from OpenAPI spec)
- **Build**: esbuild (CJS bundle)

## Key Commands

- `pnpm run typecheck` — full typecheck across all packages
- `pnpm run build` — typecheck + build all packages
- `pnpm --filter @workspace/api-spec run codegen` — regenerate API hooks and Zod schemas from OpenAPI spec
- `pnpm --filter @workspace/db run push` — push DB schema changes (dev only)
- `pnpm --filter @workspace/api-server run dev` — run API server locally

See the `pnpm-workspace` skill for workspace structure, TypeScript setup, and package details.
